<% if flash[:notice] %>
  <div>
    <%= flash[:notice] %>
  </div>
<% end %>

<div>
  <fieldset>
    <%= form_tag(login_path, method: "post") do %>
        <p><%= label_tag(:email, "E-mail") %></p>
        <p><%= email_field_tag(:email) %></p>

        <p><%= label_tag(:password, "Password") %></p>
        <p><%= password_field_tag(:password) %></p>
        <p><%= submit_tag("Log in") %></p>

    <% end %>
  </fieldset>

  <%= link_to "Register", new_user_path %>
</div>